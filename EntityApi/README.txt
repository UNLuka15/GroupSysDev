To generate the database, open the solution and run the following command in the package manager:

"update-database -Verbose"

This will generate a blank database at (localdb)\mssqllocaldb called MuseumDev which can be accessed in Microsoft SQL Server Management Studio.
